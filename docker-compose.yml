services:
  m365bridge:
    build: .
    image: m365bridge-fork:local
    pull_policy: build
    container_name: m365bridge
    ports:
      - "8230:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
